# AI 农批开单员 — 项目规则（最高优先级）

你正在开发 Voice-first 行业 AI Agent（AI Employee Runtime 的第一个员工）。本文件对所有 AI 辅助开发强制生效。

## 设计依据

0. `docs/AI_EMPLOYEE_ARCHITECTURE.md` 是后续所有开发决策的**最高架构约束**（分层、LLM 权限、Policy/Memory 冻结、ERP 只经 Outbox Adapter）。每个 Sprint / PR 开头必须声明：遵守该文件、本 Sprint 只允许修改哪一层。禁止只写「做 Sprint X」。设计评审必须回答该文件 §7 六问。
1. `docs/DESIGN.md` 是农批开单员的产品设计细则。实现、重构、加字段前必须先读对应章节。与架构文冲突时：先守分层与权限，再改 DESIGN。
2. 修改核心架构之前：必须先修改架构文和/或 `docs/DESIGN.md`，再改代码。发现设计缺陷同样：先文档，后代码。
3. 不允许为了快速实现破坏领域模型（禁止扁平 SKU、单意图、全局唯一客户名、澄清即停图等捷径当成长期架构）。
4. 重大架构决策必须写入 `docs/ADR/`，且不得与架构文 / DESIGN.md 打架。

## 技术边界

5. LLM 不能直接决定业务规则。LLM 只理解语言、抽取 `SpeechAct[]`。是否询问、是否自动执行、是否确认由 Policy 决定。禁止选客户、选 SKU、定价、写 Memory、判断 Confirm。
6. LLM 不能直接访问数据库。禁止在 prompt 或 Agent 节点里拼 SQL。
7. Agent 不允许直接操作 ORM，禁止 import `app.models` / `app.database`。
8. 所有业务执行必须：

```
Agent → Tool → Service → Repository → Database
```

接 ERP 只能：`Domain Event → Outbox → ERPNext Adapter`。禁止 Domain Service 直调 ERP API。Runtime 不读 ERP 表，ERP 失败不得改确认结果。

## 上下文与记忆

9. Memory 不是聊天记录。必须从确认后的结构化事件学习。禁止 `user_text` 学习、禁止 LLM 直接写 Memory。
10. Session 是一张订单（当前 SalesSession），不是 IM 会话。禁止聊天记录、禁止跨订单上下文。必须考虑未来 Purchase / Payment / Inventory 经事件扩展，禁止在销售确认里扣库存或收款。

## 扩展

11. 所有新增功能必须考虑未来行业扩展（农批 → 装修 / 汽修 / 物流），共享 SpeechAct、Memory、Policy、Entity Resolver，不把行业特例写死进 Agent 图。当前不是做多 Agent、不是做平台。
