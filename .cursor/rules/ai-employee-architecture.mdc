---
description: AI Employee Runtime 全局架构是最高分层约束；Sprint 必须声明只改哪一层
alwaysApply: true
---

# AI Employee Runtime

先读 `docs/AI_EMPLOYEE_ARCHITECTURE.md`。这是后续所有开发决策的最高架构约束。

本项目不是普通订单系统，也不是聊天机器人。ERPNext 是企业事实系统；AI Runtime 是自然语言业务执行层。禁止把 AI 做成 ERP 聊天框，禁止把 ERP 逻辑塞进 Runtime。

## 分层（不得打乱）

```text
Input Adapter → Turn Intake → Parser → Product Understanding
  → Resolver → Policy → Domain Service → Event → Outbox
  → Memory | ERPNext Write Adapter
装配层 Domain Query → ERPNext Read Adapter
```

- Parser 只理解语言，禁止选客户 / 选 SKU / 定价 / 写 Memory / 判断 Confirm。
- Understanding 只理解货语，禁止直接输出 SKU。
- Resolver 只识别已有 Catalog，禁止猜、禁止建 SKU。
- Policy 是业务安全边界；LLM 不能替代或绕过。
- Memory 只从确认后的结构化事件学习，禁止 `user_text`、禁止 LLM 直写。
- `SalesSession` = 一张订单；禁止聊天记录、禁止跨单上下文。
- 写 ERP：`Event → Outbox → Write Adapter`。读 ERP：装配层领域查询 → Read Adapter。禁止 `OrderService` / Policy 直调 ERP API，禁止 SQL/DocType 进 Runtime。

## 每个 Sprint / PR 开头必须声明

```text
当前遵守 docs/AI_EMPLOYEE_ARCHITECTURE.md。
本 Sprint 只允许修改：<层名>。
禁止修改：<冻结层>。
```

禁止只写「做 Sprint X」。

## 设计评审六问（缺一不可）

1. 这个能力属于哪一层？
2. 是否改变 LLM 权限？
3. 是否绕过 Policy？
4. 是否污染 Memory？
5. 是否应该通过 Event 连接？
6. 是否属于 ERPNext Adapter，而不是 Runtime？

不确定时优先冻结 Runtime。当前不做库存/支付/财务过账、不做多 Agent、不做平台。
