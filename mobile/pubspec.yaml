name: sales_clerk
description: 农批 AI 销售开单员 V1。Flutter 今日开单本壳，只调现有 Runtime API。
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  shared_preferences: ^2.5.3
  uuid: ^4.5.1
  flutter_tts: ^4.2.3
  permission_handler: ^11.4.0
  sherpa_onnx: ^1.12.23
  record: ^6.0.0
  path_provider: ^2.1.5
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/asr/tokens.txt
    - assets/asr/model.int8.onnx
